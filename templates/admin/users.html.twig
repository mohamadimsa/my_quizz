{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
    <h1>Liste des utilisateurs</h1>
{% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('register_user') }}">REGISTER USER</a>
{% endif %}
    <table>
        <thead>
            <th>ID</th>
            <th>Email</th>
            <th>Rôles</th>
            <th>Modifier</th>
            <th>Supprimer</th>
            <th>Options</th>
        </thead>
        <tbody>
            {# On boucle sur les utilisateurs #}
 
            {% for user in users %}

                <tr>
                {% if app.user.email == user.email%}
                <p>Vous etes connecté avec l'admin {{ user.email }}</p>
                {% else %}
                    <td>{{ user.id }}</td>
                    <td>{{ user.email }}</td>
                    <td>
                        {# On boucle sur les rôles #}
                        {% for role in user.roles %}
                            {% if role == "ROLE_USER" %}
                                Utilisateur
                            {% elseif role == "ROLE_EDITOR" %}
                                Editeur
                            {% elseif role == "ROLE_ADMIN" %}
                                Administrateur
                            {% endif %}
                        {% endfor %}
                    </td>
                    <td><a href="{{ path('modifier_utilisateur', {'id': user.id}) }}">Modifier</a></td>
                     <td><a href="{{ path('supprimer_utilisateur', {'id': user.id}) }}">Supprimer</a></td>
                      {% if (user.ActivationToken == null)%}
                       <td><a href="{{ path('desactiver_utilisateur', {'id': user.id}) }}">Desactiver</a></td>
                       {% else%}
                         <td><a href="{{ path('activer_utilisateur', {'id': user.id}) }}">Activer</a></td>
                         {% endif %}
                </tr>
                {% endif%}
                
            {% else %}
                <p>Aucun utilisateur inscrit</p>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
